query GetPage($id: ID!) {
    page(id: $id) {
        id
        code
        title
        description
        sections(first: 100) {
            items {
                __typename
                id
                title
                description
                ...ItemSectionFragment
            }
        }
    }
}
